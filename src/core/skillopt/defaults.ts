/**
 * Default SkillOpt spend ceilings. Explicit CLI/MCP/job flags can raise these,
 * but unattended defaults stay under the autonomous daily cap.
 */
export const DEFAULT_SKILLOPT_MAX_COST_USD = 3.33;
export const DEFAULT_SKILLOPT_BRAIN_WIDE_MAX_COST_USD = 3.33;
